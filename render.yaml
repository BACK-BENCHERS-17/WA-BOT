services:
  - type: web
    name: wa-command
    env: node
    region: oregon
    plan: free
    buildCommand: npm install -g pnpm@9 && pnpm install --frozen-lockfile && pnpm --filter @workspace/whatsapp-bot run build && pnpm --filter @workspace/api-server run build
    startCommand: node artifacts/api-server/dist/index.mjs
    healthCheckPath: /api/healthz
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        fromDatabase:
          name: wa-command-db
          property: connectionString
      - key: SESSION_SECRET
        generateValue: true

databases:
  - name: wa-command-db
    databaseName: wacommand
    user: wacommand
    plan: free
